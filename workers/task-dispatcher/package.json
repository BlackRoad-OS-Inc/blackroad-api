{
  "name": "blackroad-task-dispatcher",
  "version": "1.0.0",
  "description": "BlackRoad OS — Cloudflare Worker for long-running task dispatch",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260316.1",
    "typescript": "^6.0.2",
    "wrangler": "^4.76.0"
  }
}
